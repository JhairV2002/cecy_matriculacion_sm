import { CourseListCheckboxNonfreePipe } from './course-list-checkbox-nonfree.pipe';

describe('CourseListCheckboxNonfreePipe', () => {
  it('create an instance', () => {
    const pipe = new CourseListCheckboxNonfreePipe();
    expect(pipe).toBeTruthy();
  });
});
